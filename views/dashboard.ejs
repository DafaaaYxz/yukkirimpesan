<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Inbox - NGL Clone</title>
</head>
<body class="bg-black text-white p-6 font-sans">
    <div class="max-w-4xl mx-auto">
        <div class="flex justify-between items-center mb-10">
            <h1 class="text-2xl font-black tracking-tighter">INBOX : @<%= user %></h1>
            <a href="/logout" class="bg-red-600/20 text-red-500 px-4 py-2 rounded-full text-xs font-bold border border-red-600/30">LOGOUT</a>
        </div>

        <!-- Link Box -->
        <div class="bg-[#111] border border-cyan-900/50 p-6 rounded-[2rem] mb-10">
            <p class="text-xs text-gray-500 uppercase tracking-widest mb-2">Link Profile Anda:</p>
            <div class="flex items-center justify-between bg-black p-4 rounded-2xl border border-[#222]">
                <code class="text-cyan-400 text-sm">yukkirimpesan.vercel.app/u/<%= user %></code>
            </div>
        </div>

        <!-- Messages Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <% if(messages.length == 0) { %>
                <div class="col-span-full text-center py-20">
                    <p class="text-gray-600 italic">Belum ada pesan yang masuk...</p>
                </div>
            <% } %>

            <% messages.forEach(m => { %>
                <!-- 'm' sudah otomatis jadi object, tidak perlu JSON.parse lagi -->
                <div class="bg-[#111] border border-[#222] p-6 rounded-[2rem] hover:border-cyan-500/50 transition-all group">
                    <div class="flex justify-between items-start mb-4">
                        <span class="bg-cyan-500 w-2 h-2 rounded-full shadow-[0_0_10px_cyan]"></span>
                        <span class="text-[10px] text-gray-600 font-mono"><%= new Date(m.time).toLocaleTimeString() %></span>
                    </div>
                    <p class="text-lg font-medium leading-relaxed">"<%= m.text %>"</p>
                    <p class="text-[9px] text-gray-700 mt-4 uppercase tracking-[0.2em] font-bold"><%= new Date(m.time).toDateString() %></p>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
